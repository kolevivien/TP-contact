{% extends 'base.html.twig' %}

{% block title %}Ajouter un Utilisateur{% endblock %}

{% block body %}
 <center>
    <h1>Ajouter un Utilisateur</h1>

    {{ form_start(form) }} 

         {{ form_row(form.nom) }}<br />
        {{ form_row(form.prenom) }}<br />
        {{ form_row(form.numero) }}<br />
        <button type="submit">Enregistrer</button>
<center/>
    {{ form_end(form) }}
    <br>

   <h2> <a href="{{ path('liste_utilisateurs') }}" class="btn btn-secondary">Afficher la liste des utilisateurs</a></h2>
   <br/>
   <h2> <a href="{{ path('contact') }}" class="btn btn-secondary"> Contacter us</a></h2>
{% endblock %}
